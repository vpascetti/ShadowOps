# ISS-020: API scaffold and /health

Epic: EPIC-P1
Priority: P0
Effort: M
Labels: api

## Description
Scaffold Express API with a /health endpoint.

## Acceptance Criteria
- API runs locally with a single command.
- GET /health returns status and version metadata.
- API uses TypeScript and shared tooling.
